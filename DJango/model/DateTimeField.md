# DateTimeField

### auto_now_add

해당 객체가 생성될때 현재 시간 자동 저장

```python
models.DateTimeField(auto_now_add=True)
```

### auto_now

갱신시 현재 시간 자동 저장

```python
models.DateTimeField(auto_now=True)
```
